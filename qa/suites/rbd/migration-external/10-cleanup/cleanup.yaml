tasks:
  - exec:
      cluster2.client.0:
        - rbd --cluster cluster2 snap remove client.0.0@snap || true
  - exec:
      client.0:
        - rbd migration abort client.0.0 || true
        - rbd rm client.0.0 || true
