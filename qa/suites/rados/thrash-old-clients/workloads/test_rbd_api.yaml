meta:
- desc: |
   librbd C and C++ api tests
overrides:
  ceph:
    log-ignorelist:
      - overall HEALTH_
      - \(CACHE_POOL_NO_HIT_SET\)
      - \(POOL_APP_NOT_ENABLED\)
      - is full \(reached quota
      - \(POOL_FULL\)
workload:
- workunit:
    clients:
      client.2:
        - rbd/test_librbd.sh
